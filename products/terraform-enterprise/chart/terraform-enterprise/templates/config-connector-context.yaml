{{/*
Copyright IBM Corp. 2023, 2025
SPDX-License-Identifier: MPL-2.0
*/}}

{{/*
# ConfigConnectorContext - NOT REQUIRED in Cluster Mode
#
# This file is retained for reference but no resources are created when
# Config Connector is running in cluster mode (which is required for
# GCP Marketplace deployments with dynamic namespaces).
#
# In cluster mode, a single cluster-wide controller manages all namespaces.
# The cluster-wide GSA must have:
# 1. Workload Identity binding: PROJECT.svc.id.goog[cnrm-system/cnrm-controller-manager]
# 2. IAM roles: cloudsql.admin, redis.admin, storage.admin, monitoring.metricWriter
#
# For namespaced mode (not recommended for Marketplace), you would need:
# - ConfigConnectorContext per namespace
# - Per-namespace WI binding: PROJECT.svc.id.goog[cnrm-system/cnrm-controller-manager-NAMESPACE]
*/}}
